# Frontend configuration (copied to .env in deployment; do not commit secrets)

# App title shown in header
VITE_APP_NAME="Notes"

# LocalStorage key used for persistence
VITE_STORAGE_KEY="notes_app_storage_v1"

# Default sort: "updated" or "title"
VITE_DEFAULT_SORT="updated"
